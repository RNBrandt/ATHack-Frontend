module QueryHelper
end

class SupportApi
  # def support_search
  #   call :searchSupport, {:location => location, :numberOfResults => number_of_results}.to_json
  # end


  protected
  # def call
  #   url = URI.parse("https://sf-survivor-services-api.herokuapp.com/organizations/"
  #   req = Net::HTTP::Get.new(url.request_uri)
  #   res = Net::HTTP.new(url.host, url.port).start { |http| http.request(req) }
  #   raise "HTTP error code #{res.code}" unless res.code == "200"
  #   OpenStruct.new(JSON.parse res.body)
  # end
end
